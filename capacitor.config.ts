import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.fidev.sociallogin',
  appName: 'social-login',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
